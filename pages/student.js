import React, {Fragment} from "react";
import Head from "next/head"

export default function Student() {
    return (
        <Fragment>
            <Head>
                <title>Student - Open World</title>
            </Head>
        </Fragment>
    );
}
